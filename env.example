# OpenAI Configuration
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4o-transcribe
OPENAI_TRANSCRIPTION_PROMPT="You will hear audio coming from one participant in a conference, your job is to transcribe it. Keep in mind the following: * There might be pauses during which another person speaks, you will not hear anything in this period * The person might speak any language, you need to detect it. However, it is unlikely that the language will switch often"

# Server Configuration
PORT=8080
HOST=0.0.0.0

# Transcription Configuration
FORCE_COMMIT_TIMEOUT=2

# Broadcast transcripts to other participants as context (default: false)
BROADCAST_TRANSCRIPTS=false
# Maximum size of transcript history in bytes (default: 5120 = 5 KB)
BROADCAST_TRANSCRIPTS_MAX_SIZE=5120

# WebSocket Message Dumping (for testing/replay)
# Dumps all incoming WebSocket messages to /tmp/$sessionId/websocket-messages.jsonl
DUMP_WEBSOCKET_MESSAGES=false

# Transcript Dumping (for testing/analysis)
# Dumps all transcripts to /tmp/$sessionId/transcripts.jsonl
DUMP_TRANSCRIPTS=false

# Base path for dump files (default: /tmp)
# Files will be written to $DUMP_BASE_PATH/$sessionId/
DUMP_BASE_PATH=/tmp

# Debug Mode
DEBUG=true
